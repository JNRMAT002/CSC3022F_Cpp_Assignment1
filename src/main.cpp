#include <iostream>
#include <vector>

int main (int argc, char* argv[]) {
    // std::vector<std::string> files;
    std::string input;

    getline(std::cin, input);

    std::cout << input << std::endl;
    
    return 0;
}